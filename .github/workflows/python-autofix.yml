name: Python safe autofix

on:
  workflow_dispatch:

concurrency:
  group: python-safe-autofix-main
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  autofix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - name: Install dependencies
        run: pip install -r requirements.txt -e .
      - name: Apply safe Ruff fixes and formatting
        run: |
          ruff check src tests scripts --fix || true
          ruff format src tests scripts
      - name: Capture remaining diagnostics
        run: |
          mkdir -p reports
          if ruff check src tests scripts --output-format=concise > reports/ruff-lint.txt 2>&1; then
            echo "All Ruff checks passed." > reports/ruff-lint.txt
          fi
      - name: Commit fixes and diagnostics
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if git status --porcelain src tests scripts reports/ruff-lint.txt | grep -q .; then
            git add src tests scripts reports/ruff-lint.txt
            git commit -m "style: apply Ruff fixes and diagnostics [skip ci]"
            git pull --rebase origin main
            git push origin HEAD:main
          else
            echo "No safe autofixes or diagnostic changes"
          fi
      - name: Verify lint after fixes
        run: ruff check src tests scripts
